# 小艾抓蛋仔大作战

一个纯静态网页小游戏，包含开始页、抓蛋游戏页和蛋蛋农场。

## 本地预览

```powershell
python -m http.server 5180
```

然后打开 `http://localhost:5180/`。

## GitHub Pages

把仓库发布到 GitHub Pages 时，入口文件是根目录下的 `index.html`。项目使用相对路径，适合部署在 `https://用户名.github.io/仓库名/`。
